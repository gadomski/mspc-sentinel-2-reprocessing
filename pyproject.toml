[project]
name = "mspc-sentinel-2-reprocessing"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "obstore>=0.9.2",
    "pystac>=1.14.3",
    "pystac-client>=0.9.0",
]
